import { FocusformDirective } from './focusform.directive';

describe('FocusformDirective', () => {
  it('should create an instance', () => {
    const directive = new FocusformDirective();
    expect(directive).toBeTruthy();
  });
});
